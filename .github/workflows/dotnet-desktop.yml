name: .NET Core Desktop

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: windows-latest 

    steps:
      - name: debug
        uses: namespacelabs/breakpoint-action@v0
        with:
          duration: 10m
          authorized-users: sht2017

    # # Install dependencies
    # - name: Install .NET Core
    #   run: |
    #     echo ${{ github.workspace }}
    #     echo $GITHUB_WORKSPACE
    #     echo $pwd
